version: 2
updates:
  # github-actions only: the plugin is shell + jq, with no package manifest to track.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(ci)"
